'use client';
import {useEffect,useState} from 'react';
import {supabase} from '../../lib/supabase';
export default function Notifications(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const load=async()=>{if(!supabase)return;const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data}=await supabase.from('notifications').select('*').eq('user_id',user.id).order('created_at',{ascending:false});setRows(data||[])};load()},[]);return <main className="section"><div className="container"><a className="back" href="/dashboard">← لوحة التحكم</a><h1>الإشعارات</h1><p className="muted">آخر التنبيهات المتعلقة بطلباتك ومشاريعك.</p>{rows.length===0?<div className="panel empty"><div className="icon">🔔</div><h2>لا توجد إشعارات</h2><p className="muted">ستظهر هنا عند وصول تحديثات جديدة.</p></div>:<div className="panel">{rows.map(n=><div className="activity" key={n.id}><div><strong>{n.title||'إشعار جديد'}</strong><p className="muted">{n.body||n.message||''}</p></div><small>{new Date(n.created_at).toLocaleDateString('ar-DZ')}</small></div>)}</div>}</div></main>}
