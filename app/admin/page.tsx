'use client';
import {useEffect,useState} from 'react';
import {supabase} from '../../lib/supabase';
export default function Admin(){const [stats,setStats]=useState({users:0,services:0,projects:0,orders:0});const [allowed,setAllowed]=useState(false);const [loading,setLoading]=useState(true);useEffect(()=>{const load=async()=>{if(!supabase){setLoading(false);return}const {data:{user}}=await supabase.auth.getUser();if(!user){setLoading(false);return}const {data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).single();if(profile?.role!=='admin'){setLoading(false);return}setAllowed(true);const count=async(table:string)=>{const {count}=await supabase.from(table).select('*',{count:'exact',head:true});return count||0};setStats({users:await count('profiles'),services:await count('services'),projects:await count('projects'),orders:await count('orders')});setLoading(false)};load()},[]);if(loading)return <main className="section"><div className="container"><div className="panel empty">جارٍ تحميل لوحة الإدارة…</div></div></main>;if(!allowed)return <main className="section"><div className="container"><div className="panel empty"><div className="icon">🔒</div><h2>غير مصرح</h2><p className="muted">هذه الصفحة مخصصة لمسؤول المنصة.</p></div></div></main>;return <main className="section"><div className="container"><div className="dashHead"><div><span className="eyebrow light">ADMIN</span><h1>لوحة إدارة سوق DZ</h1><p className="muted">مراقبة المنصة وإدارة النشاطات.</p></div></div><div className="stats"><div><span>المستخدمون</span><strong>{stats.users}</strong></div><div><span>الخدمات</span><strong>{stats.services}</strong></div><div><span>المشاريع</span><strong>{stats.projects}</strong></div><div><span>الطلبات</span><strong>{stats.orders}</strong></div></div><div className="trust"><div className="trustBox"><h3>👥 المستخدمون</h3><p className="muted">مراجعة الحسابات والأدوار والحالات.</p></div><div className="trustBox"><h3>📦 الطلبات</h3><p className="muted">متابعة الطلبات والعمولات والنزاعات.</p></div><div className="trustBox"><h3>🛠️ المحتوى</h3><p className="muted">مراجعة الخدمات والمنتجات والمشاريع.</p></div></div></div></main>}
