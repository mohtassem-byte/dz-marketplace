'use client';
import { useEffect,useState } from 'react'; import { listProducts } from '../../lib/marketplace';
export default function Products(){const [items,setItems]=useState<any[]>([]); const [loading,setLoading]=useState(true); useEffect(()=>{listProducts().then(r=>{setItems(r.data||[]);setLoading(false)})},[]); return <main className="section"><div className="container"><a className="back" href="/">← سوق DZ</a><div className="sectionHead"><div><span className="eyebrow light">🇩🇿 Marketplace الجزائر</span><h1>السلع والمنتجات</h1><p className="muted">اكتشف منتجات من البائعين في مختلف الولايات.</p></div><a className="btn primary" href="/products/new">+ أضف منتجًا</a></div>{loading?<div className="panel">جارٍ تحميل المنتجات…</div>:items.length===0?<div className="panel empty"><div className="icon">🛍️</div><h2>لا توجد منتجات بعد</h2><p className="muted">ابدأ بإضافة أول منتج إلى سوق DZ.</p></div>:<div className="cards">{items.map(p=><article className="card" key={p.id}><div className="cardTop"><div className="avatar">🛍️</div><div><strong>{p.profiles?.full_name||'بائع سوق DZ'}</strong><div className="muted">📍 {p.wilaya||'الجزائر'}</div></div></div><h3>{p.title}</h3><p className="muted">{p.description}</p><div className="price">{Number(p.price_dzd).toLocaleString('ar-DZ')} دج</div><div className="tags"><span className="tag">📦 مخزون {p.stock}</span>{p.delivery_available&&<span className="tag">🚚 توصيل متاح</span>}</div></article>)}</div>}</div></main>}
